@page
@model LogoutModel


<header>
    
    @{
        if (User.Identity.IsAuthenticated)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/Register", new { area = "" })" method="post">  
            </form>

        }
        else
        {
           
        }
    }
</header>